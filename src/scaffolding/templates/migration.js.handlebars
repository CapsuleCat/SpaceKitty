/**
 * {{#if hasNamespace}}{{namespace}} {{/if}}{{name}} Migration
 */
Migrations.add({
  version: {{timeStamp}},
  name: '{{#if hasNamespace}}{{namespace}} {{/if}}{{name}}',
  up: function () {
    //
  },
  down: function () {
    //
  }
});
